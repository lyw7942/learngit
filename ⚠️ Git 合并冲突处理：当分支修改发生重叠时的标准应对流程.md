# **⚠️ Git 合并冲突处理：当分支修改发生重叠时的标准应对流程**

## **📌 这篇文章是干什么的？**

本文讲解 **当两个 Git 分支同时修改了同一文件的相同区域时，如何正确处理合并冲突（merge conflict）**。
Git 无法自动判断应保留哪一版本，会暂停合并并标记冲突位置，**需开发者手动解决后提交**。

这是多人协作、长期功能开发或频繁并行修改中的**常见场景**，掌握冲突解决是 Git 进阶的核心能力。

------

## **🎯 使用场景**

- 多人同时编辑同一个文件（如 `README.md`、配置文件、核心模块）
- 你在 `feature` 分支开发期间，`main` 分支也被其他人更新
- 你本地 `main` 和远程 `origin/main` 出现分叉后再合并
- 自动化工具（如 Dependabot）和你同时修改了依赖文件

> 💡 冲突不是错误，而是 Git 在提醒你：“这里有分歧，请你裁决。”

------

## **🛠️ 标准操作流程（冲突 → 解决 → 完成）**

```shell
# 1. 在 feature 分支开发并提交
git switch -c feature1
# （修改文件）
git add .
git commit -m "Update content in feature"

# 2. 切回 main，也修改同一文件（制造冲突）
git switch main
# （修改同一文件的相同位置，内容不同）
git add .
git commit -m "Update content in main"

# 3. 尝试合并 → 触发冲突
git merge feature1

# 4. 查看冲突状态
git status  # 显示 "both modified: <file>"

# 5. 手动编辑冲突文件：
#    - 删除 <<<<<<< HEAD、=======、>>>>>>> 标记
#    - 保留或重写最终内容

# 6. 标记冲突已解决并完成合并
git add <conflicted-file>
git commit -m "conflict fixed"

# 7. 清理分支
git branch -d feature1
```

------

## **🚀 日常工作流 · 极简指令汇总**

```shell
# 开发新功能
git switch -c feature/xxx
# ... 修改文件 ...
git add .
git commit -m "feat: xxx"

# 合并到主干（可能冲突）
git switch main
git merge feature/xxx

# 若无冲突：直接 push
# 若有冲突：
#   1. 编辑文件，删掉冲突标记
#   2. git add .
#   3. git commit -m "fix: resolve conflict"
#   4. git push

# 清理
git branch -d feature/xxx
```

> ✅ 前提：`main` 已设置 upstream（通常通过 `git clone` 或首次 `push -u` 完成）

------

## **⚠️ 特殊情况处理**

```shell
# 中止当前合并（回到合并前状态）
git merge --abort

# 强制删除未合并的分支（慎用）
git branch -D feature1

# 查看哪些分支尚未合并到当前分支
git branch --no-merged

# 推送合并后的主干
git push

# 如果远程 main 有更新，先拉取再合并（避免额外冲突）
git switch main
git pull origin main
git merge feature/xxx
```

> 💡 提示：
>
> - 冲突文件中 `<<<<<<< HEAD` 是**当前分支**内容，`>>>>>>> other-branch` 是**被合并分支**内容
> - 解决后必须 `git add` 再 `commit`，否则 Git 认为冲突未解决
> - 使用 VS Code、IDEA 等编辑器可图形化解决冲突（点击 “Accept Current” / “Accept Incoming”）

------

> ✅ **记住口诀**：
> **“冲突不怕，标记看清；手动编辑，add 提交；合并完成，分支清理。”**