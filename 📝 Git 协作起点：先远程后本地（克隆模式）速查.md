## **📝 Git 协作起点：先远程后本地（克隆模式）速查**

### **✅ 适用场景**

- 团队项目、新项目初始化
- 需要多人从同一状态开始开发

------

### **🔧 操作步骤（HTTPS 推荐）**

```shell
# 1. GitHub 创建仓库时 ✔ 勾选 "Initialize with README"
# 2. 克隆到本地（替换用户名）
git clone https://github.com/lyw7942/仓库名.git

# 3. 进入目录，直接工作
cd gitskills
echo "new line" >> README.md

# 4. 提交并推送
git add .
git commit -m "update"
git push    # 首次需输用户名 + PAT
```